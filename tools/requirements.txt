# Dependances RUNTIME du tooling robot_control (vision + teleop + MCP d'analyse).
# Versions epinglees = celles validees au banc (Python 3.10, Windows) -> reproductible
# depuis zero via install.ps1 / install.sh. numpy est plafonne a 2.2.x sur Python 3.10
# (2.3+ exige 3.11+). opencv-python fournit cv2 (detecteurs YuNet/DNN + tracker VitTrack).
opencv-python==5.0.0.93
numpy==2.2.6
pyserial==3.5
# NB : le protocole MAVLink n'ajoute AUCUNE dependance runtime. Le module dialecte genere
# communication/mav/bamboo.py est autonome (stdlib seule ; fastcrc optionnel avec fallback
# pur-Python). pymavlink/mavgen ne sert qu'a REGENERER ce module -> cf. requirements-dev.txt.
